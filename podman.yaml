apiVersion: v1
kind: Pod
metadata:
 name: podman
spec:
 containers:
   - name: container
     image: amoldeshpande/podman:latest
     args:
       - sleep
       - "infinity"
     securityContext:
       privileged: true
